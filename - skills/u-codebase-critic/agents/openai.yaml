interface:
  display_name: "U · Codebase Critic"
  short_description: "User-invoked exhaustive repository audit"
  default_prompt: "Use $u-codebase-critic to perform an exhaustive, assessment-only audit of this repository and write one canonical evidence-backed report without modifying code."
policy:
  allow_implicit_invocation: false
